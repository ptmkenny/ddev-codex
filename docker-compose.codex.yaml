#ddev-generated
# OpenAI Codex CLI installation and persistent state.

services:
  web:
    build:
      args:
        CODEX_VERSION: ${CODEX_VERSION:-latest}
    volumes:
      - ./codex:/mnt/codex-config
