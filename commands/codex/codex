#!/usr/bin/env bash
#ddev-generated
## Description: Run OpenAI Codex CLI in the isolated Codex sidecar
## Usage: codex [args]
## Example: "ddev codex" or "ddev codex --version"
## MutagenSync: true

if ! command -v codex >/dev/null 2>&1; then
  echo "Codex CLI is not installed. Run 'ddev restart --no-cache' to rebuild the project image." >&2
  exit 1
fi

exec codex "$@"
